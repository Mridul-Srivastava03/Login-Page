# Application Name
spring.application.name=permitbackend

# ====================
# ? PostgreSQL (Aiven)
# ====================
spring.datasource.url=jdbc:postgresql://permitdb-mitu1093-7de1.c.aivencloud.com:28501/defaultdb?sslmode=require
spring.datasource.username=avnadmin
spring.datasource.password=AVNS_Cofk63JFjg7s0dWK83q
spring.datasource.driver-class-name=org.postgresql.Driver

# ====================
# ? JPA / Hibernate
# ====================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ====================
# ? Server
# ====================
server.port=8080
