# Application Name
spring.application.name=permitbackend

# ====================
# ? PostgreSQL (Aiven)
# ====================


# ====================
# ? JPA / Hibernate
# ====================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ====================
# ? Server
# ====================
server.port=8080
